<?php
defined('BASEPATH') OR exit('No direct script access allowed');

/**
 * Helper to verify if the user is logged in
 */

// ------------------------------------------------------------------------

if ( ! function_exists('verify_user_session'))
{
	/**
	 * verify_user_session
	 *
	 * Verify if the user is logged in to display the facebook login button
	 */
	function verify_user_session()
	{
	}
}
